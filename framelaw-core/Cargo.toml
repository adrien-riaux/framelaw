[package]
name = "framelaw-core"
version = "0.1.0"
edition = "2021"

[dependencies]
polars = { version = "0.41", default-features = false, features = ["lazy", "strings", "regex"] }
rayon = "1.10.0"
regex = "1.10.4"
jiter = "0.5"
